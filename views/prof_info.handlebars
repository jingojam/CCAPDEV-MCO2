
<div class="container">
    <div class="create_options">
            <a href="/home">
                <div id="option_1">
                    <p>< Return to Home</p>
                </div>
            </a>
        </div>

    <div class="profile-reservations-wrapper">

        <!-- Profile Section -->
        <div class="profile-section">
            <div class="profile-info">
                <img src="{{user.image}}" alt="Header Profile Picture" class="profile-pic">
                <h1>{{user.name}}</h1>
                <div class="description-box">
                    <p>{{user.bio}}</p>
                </div>
            </div>

            <div class="manage-profile">
                <button id="edit-button" class="button" 
                    onclick="location.href='/prof_edit'">Edit Profile</button>
            </div>
        </div>

        <!-- Reservations Section -->
        <div class="reservations-info">
            <h1>Current Reservations</h1>
            <table class="reservation-table">
                <thead>
                    <tr>
                        <th>Laboratory</th>
                        <th>Seat</th>
                        <th>Date & Time</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each reservations}}
                        <tr>
                            <td>{{lab}}</td>
                            <td>{{seat}}</td>
                            <td>{{datetime}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

    </div>
</div>

